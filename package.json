{
  "name": "sst-python-boilerplate",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "dependencies": {
    "@aws-cdk/aws-lambda-python-alpha": "^2.17.0-alpha.0",
    "@serverless-stack/cli": "0.69.2",
    "@serverless-stack/resources": "0.69.2",
    "aws-cdk-lib": "2.15.0"
  }
}
